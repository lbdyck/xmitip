)attr default(%+_)
` type(text) intens(high) caps(off) just(asis ) color(yellow)
~ type(text) intens(high) caps(off) just(asis ) color(turq)
% type(text) intens(high)
)Body Window(55,9) Expand(\\)
%-\-\- ~XMITIP Zip Setting%-\-\-
+
+This option will cause the ZIP settings PopUp Panel
+to be displayed.
+
+Valid options are:
+
~Yes+~No+or~Never
)Init
 &zup = xmitiph0
)End
