)attr default(%+_)
` type(text) intens(high) caps(off) just(asis ) color(yellow)
~ type(text) intens(high) caps(off) just(asis ) color(turq)
% type(text) intens(high)
)Body Window(58,7) Expand(\\)
%-\-\- ~QUIET ISPF Option%-\-\-
+
~QUIET when set to`Yes~will only display the XMITIP Log
~when the return code is non-zero.
~
~When set to`No~(default) the XMITIP Log will
~always display.
)Init
 &zup = xmitiph0
)End
