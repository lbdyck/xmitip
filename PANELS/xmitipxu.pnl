)attr default(%+_)
` type(text) intens(high) caps(off) just(asis ) color(turq)
~ type(text) intens(high) caps(off) just(asis ) color(yellow)
% type(text) intens(high)
)Body Window(60,11) Expand(\\)
%-\-\- ~XMITIP PDF Index%-\-\-
+
`Use this option to define indexing for the PDF attachment.
+
`The format is:`row/column/length
+
`e.g.~  2/2/15
+
`If row = 0 then the column and length will be checked for
`all rows.
)Init
 &zup = xmitiph0
)End
