)attr default(%+_)
~ type(text) intens(high) caps(off) just(asis ) color(yellow)
` type(text) intens(high) caps(off) just(asis ) color(turq)
% type(text) intens(high)
)Body Window(60,9) Expand(\\)
%-\-\- ~XMITIP NOSTRIP%-\-\-
+
`Use this option to NOT remove trailing blanks from file
`attachment records.
+
`Valid options are:~Yes`= Do NOT strip trailing blanks
+                  ~No `= Strip Trailing Blanks
+
)Init
 &zup = xmitiph0
)End
